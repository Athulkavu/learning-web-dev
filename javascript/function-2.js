
//fn expressions
//when fn are assigned to a variable as a variable
const print=function (name){//anonymous fns ->this was used in es6 now use ***arrow fn for anonymous 
    // a("athul");
    return "hi, "+name;
}
console.log(print("name"));
